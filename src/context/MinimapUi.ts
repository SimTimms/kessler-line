/**
 * True while the HTML star chart is covering the scene (fullscreen and/or dock assist).
 * Scene direction-indicator Html labels read this to avoid overlapping the map.
 */
export const minimapOverlayActiveRef = { current: false };
