/** Toggled by the MAGNETIC button in App.tsx; read by MagneticHUD. */
export const magneticOnRef = { current: true };
export const magneticScanRangeRef = { current: 2000 }; // world units; 0 = off — matches initialPower: 2 → MAGNETIC_RANGES[1]
