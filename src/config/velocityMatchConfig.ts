/** World velocity dead zone before firing thrusters (same units as ship velocity). */
export const VEL_MATCH_DEAD = 0.45;
/** Extra hysteresis band to reduce chatter once inside the band (unused in v1; reserved). */
export const VEL_MATCH_HYST = 0.12;
