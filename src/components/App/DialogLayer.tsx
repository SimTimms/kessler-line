import { memo } from 'react';

interface DialogLayerProps {
  docked: boolean;
  dockedStation: string | null;
}

const DialogLayer = memo(function DialogLayer(_props: DialogLayerProps) {
  return <></>;
});

export default DialogLayer;
